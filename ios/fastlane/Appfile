app_identifier("com.anotherit.quadcraft")

# Only needed as a fallback for actions that don't accept the API key
# (deliver/pilot/etc. use app_store_connect_api_key below instead).
# apple_id(ENV["FASTLANE_APPLE_ID"])
# team_id(ENV["FASTLANE_TEAM_ID"])
